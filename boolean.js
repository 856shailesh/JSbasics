let age = 1;
console.log(Boolean(age))
console.log(typeof(age))

// Imp : inplace conversion doesn't haapen
//you need to catch it in new variable
let isValid = Boolean(age);
console.log(isValid)
console.log(typeof(isValid))