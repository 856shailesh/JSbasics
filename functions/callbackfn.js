//currently it's upto calculate whether to call sum or not
//if it doens't call sum , it will not execute even if we have
//passed sum as a parameter.
function caluclate(a,b , fn){
    //fn(a,b); 
}
function sum(a,b){
    return a+b;
}
caluclate(2,3,sum);